; Local-only PHP ini overrides for legacy replay container.
; Goal: keep HTTP responses clean (no warning/deprecated output) so golden baselines
; can be recorded/compared deterministically.

display_errors = Off
html_errors = Off
log_errors = On
error_reporting = E_ALL & ~E_DEPRECATED & ~E_USER_DEPRECATED

