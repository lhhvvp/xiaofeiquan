# Golden case 运行所需环境变量示例（请复制为 `.env.golden` 并按环境填写）
#
# 用法：
#   set -a
#   source .env.golden
#   set +a
#
# 然后再执行 scripts/run_golden_cases.py
#
# 如果你使用仓库自带的 `docker-compose.rewrite.yml`（MySQL 初始化脚本包含 `03-seed-dev-auth.sql`），
# 可直接使用下面的 dev 默认值跑通“录制 baseline / 回放验收”链路。

# api（小程序）
API_TOKEN="dev.dev.dev"
API_USERID="1"

# window（窗口/售票员）
WINDOW_TOKEN="dev.dev.dev"
WINDOW_UUID="dev-window-uuid"

# selfservice（自助机）
SELFSERVICE_TOKEN="dev.dev.dev"
SELFSERVICE_NO="dev-selfservice-no"
