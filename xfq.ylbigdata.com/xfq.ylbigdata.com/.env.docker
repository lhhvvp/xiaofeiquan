; 本地 docker 环境变量（legacy PHP 回放用）
;
; ThinkPHP 的 env() 会把 `foo.bar` 转成 `FOO_BAR`，因此这里使用 INI section
; 语法来生成 `CACHE_DRIVER` / `DATABASE_HOSTNAME` 等变量。

[app]
debug=false

[database]
driver=mysql
type=mysql
hostname=mysql
database=xfq_v2
username=xfq
password=xfq
hostport=3306
charset=utf8mb4
prefix=tp_
debug=false

; 避免依赖 ext-redis：本地用 file cache + file session
[cache]
driver=file
prefix=PRO_

[session]
type=file
store=file

; Dev-only rewrite helpers (offline golden replay)
[rewrite]
mock_wechat_pay=1
mock_wechat_refund=1
mock_sms=1
mock_identity=1
debug_meituan_sign=0
