module.exports = {
  // 建议把 baseUrl 放到 local.js（不提交到仓库），避免误连生产环境。
  // baseUrl: 'http://test.xfq.ylbigdata.com/api',

  // 没有真实后端接口时，可先开启 mock（本地模拟接口返回，便于继续重构与自测）。
  // mock: true,

  // mockPayment 开启后会跳过 wx.requestPayment（仅用于开发/自测）。
  // mockPayment: true,
}

